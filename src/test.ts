function main(): void {

    const value = add(2, 4);


    writeLine("test", 10, "Rot");
    writeLine("test", 10, "Blue");
    writeLine("test", 10, "Rot");
    writeLine("test", 10, "Rot");
    writeLine("test", 10, "Rot");
    writeLine("test", 10, "Rot");

}

function add(number1: number, number2: number) {

    const result = number1 + number2;

    return result;

}

function writeLine(text: string, sleep: number, color: string) {
    // CODE




    return

}